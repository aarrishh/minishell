arimanuk@c2r1s5:~/Desktop/minishell_new$ echo hi > /etc/passwd
bash: /etc/passwd: Permission denied

🌸 minishell ✦ echoecho hi > /etc/passwd
open: Permission denied




Redirections
echo hello > file (we have leak)
cat < file (we have leak)
cat << EOF then type input (heredoc) (not done yet)
ls >> file (append redirection) (we have leak) 
nosuchcommand > out (failed command with redirect) (we have leak when running exit)
👉 Redirections allocate filenames, open fds, and can leave leaks if not freed.


